spring:
  datasource:
    url: jdbc:mysql://localhost:3306/admission_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 20050530

# ↓↓↓ 在下面加上这段 ↓↓↓
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.admission.entity